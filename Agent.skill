# DualSenseKit Agent 工作规则

每次接到 DualSenseKit 开发任务时，必须先阅读 `docs/DualSenseKit-Roadmap.md`，再开始修改代码。

强制流程：

1. 任务历史状态读取
   - 确认当前分支、HEAD、工作区状态。
   - 阅读《长程任务指南》的当前基线、相关功能点、已知限制、后续决策记录。
   - 明确本次任务会影响哪些功能点。

2. 记录正在做这件事情
   - 开发前更新《长程任务指南》的“当前开发状态”。
   - 将本次任务记录为 `in_progress`。
   - 写清目标、相关功能点、开始提交、预期验证方式。

3. 开发之后的状态重写
   - 开发完成后再次更新“当前开发状态”。
   - 将任务标记为 `done` 或 `blocked`。
   - 记录完成内容、测试结果、未解决问题、下一步建议。

4. 状态灯硬件限制
   - PlayStation/DualSense 状态灯是白色 player LEDs。
   - 状态灯不支持颜色。
   - 状态灯不支持线性亮度。
   - 状态灯亮度只允许 3 档梯度，不允许做颜色选择器、线性滑块或呼吸动画。

5. 同步文档
   - 任何 UI、API、HID 协议、事件模型、硬件限制、测试流程变化，都必须同步更新 `docs/DualSenseKit-Roadmap.md`。
   - 不允许只改代码而不更新指南。

